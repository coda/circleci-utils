{
    "message": "[CircleCI] [#\/(${CIRCLE_PREVIOUS_BUILD_NUM})]: workflow \/(${CIRCLE_BRANCH}) stage \/(${CIRCLE_STAGE}) job \/(${CIRCLE_JOB})",
    "alias": "${ALIAS}",
    "description":"Compare with last passing commit: '$DIFF_URL'",
    "username": "${CIRCLE_USERNAME}",
    "user": "CI Orb",
    "source": "${CIRCLE_BUILD_URL}",
    "note": "Successful pass close alert",
    "details": {
      "build_number":"${CIRCLE_PREVIOUS_BUILD_NUM}",
      "build_url": "${CIRCLE_BUILD_URL}",
      "user": "$USER_EMAIL",
      "outcome": "success"
    }
}