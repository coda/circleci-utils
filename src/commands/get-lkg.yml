# description: >
#   Get the commit 

# steps:
#   - run:
#       name: Get diff url
#       command: |
#         LKG_HEAD=$(PIPENV_IGNORE_VIRTUALENVS=1 pipenv run python3 scripts/get-lkg-hash.py --branch-name ${CIRCLE_BRANCH} --job-name ${CIRCLE_JOB})
#         DIFF_URL="$CIRCLE_REPOSITORY_URL/compare/$LKG_HEAD...$CIRCLE_SHA1"
#         echo "export DIFF_URL=${DIFF_URL}" >> $BASH_ENV